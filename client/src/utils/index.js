
export const getApiHost = () => {
    // const host = window.location.protocol + "//" + window.location.hostname;
    // const port = process.env.PORT || 3009;
    // const address = host + ':' + port;

    const address = 'http://720b8efc.ngrok.io';
    return address;
};
